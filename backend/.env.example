# 複製成 backend/.env 後填入自己的值（.env 不進 git）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=login_mockup

# 簽 session cookie 用，產生方式：python3 -c 'import secrets;print(secrets.token_urlsafe(32))'
SESSION_SECRET=
